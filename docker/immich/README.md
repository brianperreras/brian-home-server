# Immich Compose Project

Do not treat a copied compose file as permanently current. Download the latest official `docker-compose.yml` and `example.env` from the current Immich release, then apply the paths documented in `docs/06-Immich.md`.

Keep the real `.env` on the server and out of Git.
